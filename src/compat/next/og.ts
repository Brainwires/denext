/**
 * `next/og` compat — denext's `ImageResponse` (render JSX to a PNG).
 * @module
 */
export { ImageResponse } from "../../server/mod.ts";
export type { ImageResponseOptions } from "../../server/mod.ts";
