/**
 * `next/image` compat — denext's `<Image>` as the default export.
 * @module
 */
export { Image as default } from "../../../mod.ts";
export type { ImageLoader, ImageLoaderProps, ImageProps } from "../../../mod.ts";
