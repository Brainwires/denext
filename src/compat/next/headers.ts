/**
 * `next/headers` compat — request `cookies()`, `headers()`, and `draftMode()`,
 * re-exported from denext's server runtime.
 * @module
 */
export { cookies, draftMode, headers } from "../../server/mod.ts";
