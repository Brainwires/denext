// The React version denext's compat modules REPORT (`React.version`, `ReactDOM.version`).
// One constant, pinned to the real-React baseline the parity harness tracks
// (tests/fixtures/react-surface.baseline.json → versions.react); the parity test asserts they agree.

/** The React release whose public surface denext's compat layer mirrors. */
export const REACT_COMPAT_VERSION = "19.2.8";
