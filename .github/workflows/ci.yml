name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Deno
        uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Format check
        run: deno fmt --check

      - name: Lint
        run: deno lint

      - name: Type check
        run: deno check cli.ts mod.ts

      - name: Test
        # Excludes the e2e suite (real headless Chromium; run via
        # `deno task test:e2e`); --unstable-kv enables the Deno KV store tests.
        run: deno test -A --unstable-kv --ignore=tests/e2e/

      - name: Doc lint (JSR documentation coverage)
        run: deno doc --lint mod.ts src/server/mod.ts src/client/mod.ts src/jsx/jsx-runtime.ts src/lint/denext-plugin.ts
