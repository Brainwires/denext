{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "denext.config",
  "description": "Project configuration exported from `denext.config.{ts,js}` (as `default` or named). Generated from the `DenextConfig` TypeScript type by scripts/gen-config-schema.ts; unknown top-level and `experimental.*` keys are rejected here because denext warns on them at runtime and ignores them.",
  "type": "object",
  "properties": {
    "mode": {
      "description": "Rendering mode. Omit (the default) for the App Router (SSR/SSG) pipeline. `\"spa\"` builds SpaConfig.entry as a pure client-side-rendered app — React but not Next: no `app/` directory, no SSR, no Flight. denext bundles the entry, emits an HTML shell around it, serves it with a history-API fallback, and (via `denext export` / `deno desktop`) packages it as a static app. Use it to host an existing Vite-style React SPA on denext's toolchain and runtime.",
      "enum": [
        "spa"
      ]
    },
    "spa": {
      "description": "SPA-mode settings (required when DenextConfig.mode is `\"spa\"`).",
      "type": "object",
      "properties": {
        "entry": {
          "description": "Client entry module that mounts the app, relative to the project root (e.g. `\"./src/main.tsx\"`). It is imported for its side effects — it is expected to create a root and render into `#${rootId}` on load.",
          "type": "string"
        },
        "rootId": {
          "description": "Element id the generated shell exposes for the app to mount into. Default `\"root\"`.",
          "type": "string"
        },
        "title": {
          "description": "`<title>` for the generated shell. Default `\"denext app\"`.",
          "type": "string"
        },
        "head": {
          "description": "Extra raw HTML injected into the shell `<head>` (meta tags, preconnect links, …). A `<meta name=\"viewport\">` here replaces the shell's default (`width=device-width, initial-scale=1`) — e.g. `viewport-fit=cover` for iOS safe areas.",
          "type": "string",
          "x-denext": {
            "widget": "textarea"
          }
        },
        "loading": {
          "description": "Raw HTML rendered INSIDE the mount element (`#${rootId}`) in the generated shell — the boot placeholder shown before the client bundle loads and renders. The framework does not touch it; the app's first render (createRoot/hydrateRoot) replaces `#root`'s children, clearing it. Use it to paint instantly instead of a blank page: a themed background (pair with a pre-paint script in head), a logo splash, or a spinner — the same role a Vite/CRA `index.html` fills with markup inside `<div id=\"root\">…</div>`. `denext migrate --from vite` carries the source `index.html`'s `#root` content here.",
          "type": "string",
          "x-denext": {
            "widget": "textarea"
          }
        },
        "lang": {
          "description": "`<html lang>` value for the generated shell. Default `\"en\"`.",
          "type": "string"
        },
        "env": {
          "description": "Compile-time `import.meta.env` values (the SPA analogue of a Vite `define` block). Each `{ KEY: \"value\" }` replaces `import.meta.env.KEY` with the literal `\"value\"` at build time — the way a Vite app reads `import.meta.env.VITE_*`. Only applied when the app builds through the next-compat (esbuild) pipeline (i.e. it uses npm React); a denext-native SPA has no `import.meta.env`.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "csp": {
          "description": "Content-Security-Policy for the generated shell. A client-only React SPA (Vite/CRA and denext alike) ships no CSP by default — it's the app's/host's call — so this is **opt-in**: - unset / `\"off\"` — no CSP (default). - `\"strict\"` — denext's strict policy (`default-src 'self'`, `script-src 'self'`, `object-src 'none'`, `base-uri 'self'`, `img/font 'self' data:`, and `style-src-attr 'unsafe-inline'` so React `style={{}}` keeps working). - a CspSetting object — that strict policy plus your global opt-ins (e.g. `{ connectSrc: [\"https://api.example.com\"] }` for your API host).",
          "anyOf": [
            {
              "enum": [
                "strict"
              ]
            },
            {
              "enum": [
                "off"
              ]
            },
            {
              "type": "object",
              "properties": {
                "scriptSrc": {
                  "description": "Extra `script-src` sources (external script hosts).",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "styleSrc": {
                  "description": "Extra `style-src` sources (external stylesheet hosts).",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "imgSrc": {
                  "description": "Extra `img-src` sources (`<Image>` is already same-origin via the optimizer).",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "connectSrc": {
                  "description": "Extra `connect-src` sources (fetch/XHR/EventSource/WebSocket targets).",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              }
            }
          ]
        },
        "proxy": {
          "description": "Reverse-proxy selected path prefixes to a separate backend while serving the SPA (`denext start` in `mode:\"spa\"`, and the `deno desktop` runtime). Mirrors a Vite dev server's `server.proxy`: the client talks to its own origin and denext relays matched requests — HTTP and WebSocket — to the backend. Omit for a backend-less SPA.",
          "type": "object",
          "properties": {
            "prefixes": {
              "description": "Path prefixes forwarded to target, matched against the start of the request pathname — a prefix matches the exact path and any sub-path (e.g. `/api` matches `/api` and `/api/users`). Everything else is served from the SPA export.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "target": {
              "description": "Backend origin matched requests are forwarded to (e.g. `\"http://127.0.0.1:3773\"`). HTTP is relayed via `fetch` (cookies passed through, `Set-Cookie` `Domain`/`Secure` stripped so they bind to the proxy origin over http); a WebSocket upgrade is bridged to the backend with the request `Cookie` forwarded on the handshake.",
              "type": "string"
            },
            "allowNonLoopback": {
              "description": "Permit a non-loopback target. Default `false` (loopback-only).",
              "type": "boolean"
            }
          },
          "required": [
            "prefixes",
            "target"
          ]
        },
        "precompress": {
          "description": "Write a gzip `.gz` sibling next to each compressible client asset at `build` and `export`, so `denext start` serves `Content-Encoding: gzip` with no per-request CPU. Default `true`. Set `false` when the export is bundled into a native shell such as Capacitor, whose iOS/Android webview loads files from the app bundle and never requests the `.gz` variants, to keep them out of the bundle.",
          "type": "boolean"
        },
        "ota": {
          "description": "Stamp the static export for over-the-air UI updates: `denext export` writes `_denext/ota.json` (every file's path, SHA-256 and size, plus a `version` hashed over them) into the export directory as its last step. A Capacitor shell with the `DenextOta` plugin (`denext mobile add-ota`) reads the bundled copy to know which UI it ships, and `checkForUiUpdate` from `denext/mobile` compares a server's copy against it. Default `false`. `*.gz` siblings are never listed. When the `DENEXT_OTA_SIGNING_KEY` env var holds a PKCS#8 PEM (`denext ota keygen`), the manifest is also signed.",
          "type": "boolean"
        },
        "desktop": {
          "description": "`deno desktop` packaging settings (used when building the desktop app).",
          "type": "object",
          "properties": {
            "icon": {
              "description": "Path to the app icon (relative to the project root, e.g. `\"./assets/icon.png\"` — point it anywhere, regardless of name or location). **Overrides icon auto-detection.** A **PNG is used verbatim** (supply a finished 1024² master already shaped for macOS — the app's own icon set is the right source); a JPEG/WebP is composed into the macOS template; an undecodable format (`.ico`/`.icns`) is refused with a message and the build falls back to auto-detection. When unset, denext auto-detects a web icon (`apple-touch-icon`, a named `icon`/`logo`, `favicon.png`) and, because those are small/full-bleed, composes it into Apple's macOS template (centered in the ~824px safe area of a 1024² canvas) so it isn't oversized. Either way the result is written to `desktop-icon.png` at build time.",
              "type": "string"
            }
          }
        }
      },
      "required": [
        "entry"
      ]
    },
    "i18n": {
      "description": "Internationalized routing config.",
      "type": "object",
      "properties": {
        "locales": {
          "description": "All supported locale codes, e.g. `[\"en\", \"fr\", \"de\"]`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "defaultLocale": {
          "description": "The locale served without a URL prefix, e.g. `\"en\"`.",
          "type": "string"
        },
        "localePrefix": {
          "description": "How the default locale is represented in URLs: - `\"as-needed\"` (default) — the default locale is served **unprefixed** (`/about`), every other locale is prefixed (`/fr/about`). - `\"always\"` — **every** locale is prefixed, including the default (`/en/about`); an unprefixed path redirects to the detected (or default) locale's prefix.",
          "enum": [
            "as-needed",
            "always"
          ]
        },
        "messages": {
          "description": "Optional message catalogs keyed by locale, powering `useTranslations()`. The active locale's catalog is provided to the render and embedded in the hydration payload. Server components may also load catalogs directly.",
          "type": "object",
          "additionalProperties": {}
        },
        "hreflang": {
          "description": "Automatically emit `<link rel=\"alternate\" hreflang>` alternates (one per locale, plus `x-default`) and a per-locale canonical for every page. On by default when i18n is configured; set `false` to opt out. A page that sets its own `alternates.languages` always takes precedence over the generated set.",
          "type": "boolean",
          "default": true
        },
        "domains": {
          "description": "Serve a locale per **domain** without a URL prefix (Next's `i18n.domains`): `example.fr/about` renders French with no `/fr`. Each entry pins a host to a `defaultLocale` (served unprefixed on that host); a host's other `locales` (if listed) are still prefixed there. Locales not tied to any domain keep the normal prefix behavior on the primary host. Host resolution uses the request's trusted host (honoring `trustForwardedHeaders`), never a raw `Host` header on the render path.",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "domain": {
                "description": "The host this entry matches, e.g. `\"example.fr\"` (compared case-insensitively, port-stripped).",
                "type": "string"
              },
              "defaultLocale": {
                "description": "The locale served unprefixed on this host.",
                "type": "string"
              },
              "locales": {
                "description": "Locales served on this host (prefixed, except `defaultLocale`); omit to allow all.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "http": {
                "description": "Use `http://` (not `https://`) when generating this domain's absolute URLs (local dev).",
                "type": "boolean"
              }
            },
            "required": [
              "domain",
              "defaultLocale"
            ]
          }
        }
      },
      "required": [
        "locales",
        "defaultLocale"
      ]
    },
    "basePath": {
      "description": "Serve the app under a sub-path (e.g. `/docs`). Stripped before routing.",
      "type": "string"
    },
    "trailingSlash": {
      "description": "Enforce a trailing slash on page URLs (308-redirect to normalize). Unset or `false` (Next's default) redirects `/about/` → `/about`.",
      "type": "boolean"
    },
    "assetPrefix": {
      "description": "Prefix for client bundle/asset URLs (e.g. a CDN origin).",
      "type": "string"
    },
    "redirects": {
      "description": "Declarative redirects, evaluated once at startup.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "source": {
            "description": "Path pattern to match, with `:name` and `:name*` params (e.g. `/old/:slug`).",
            "type": "string"
          },
          "destination": {
            "description": "Where to send the client; may reference `:name` params from `source`.",
            "type": "string"
          },
          "permanent": {
            "description": "`true` → 308 (permanent); otherwise 307 (temporary).",
            "type": "boolean"
          }
        },
        "required": [
          "source",
          "destination"
        ]
      },
      "x-denext": {
        "wrapper": "function"
      }
    },
    "rewrites": {
      "description": "Declarative rewrites, evaluated once at startup.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "source": {
            "description": "Path pattern to match, with `:name` params.",
            "type": "string"
          },
          "destination": {
            "description": "The path to route as instead; may reference `:name` params.",
            "type": "string"
          }
        },
        "required": [
          "source",
          "destination"
        ]
      },
      "x-denext": {
        "wrapper": "function"
      }
    },
    "headers": {
      "description": "Declarative response headers, evaluated once at startup.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "source": {
            "description": "Path pattern to match, with `:name` params.",
            "type": "string"
          },
          "headers": {
            "description": "Header name/value pairs to add to matching responses.",
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "key": {
                  "description": "The header name, e.g. `Cache-Control`.",
                  "type": "string"
                },
                "value": {
                  "description": "The value to send, e.g. `public, max-age=3600`.",
                  "type": "string"
                }
              },
              "required": [
                "key",
                "value"
              ]
            }
          }
        },
        "required": [
          "source",
          "headers"
        ]
      },
      "x-denext": {
        "wrapper": "function"
      }
    },
    "scheduledTasks": {
      "description": "Cron schedules for background tasks: a map of a 5-field cron expression to the task name(s) to run on it (a task is `tasks/<name>.ts` exporting `defineTask(...)`). Registered at server startup via `Deno.cron` when the runtime exposes it (Deno Deploy, or `--unstable-cron`) and a dependency-free minute-tick scheduler otherwise. A task may also declare its own `schedule`.",
      "type": "object",
      "additionalProperties": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "array",
            "items": {
              "type": "string"
            }
          }
        ]
      }
    },
    "tasks": {
      "description": "Scheduled/background task behaviour. Currently run history, which is off unless asked for.",
      "type": "object",
      "properties": {
        "history": {
          "description": "Record every task run — scheduled and on demand — to `.denext/tasks.db`, so `denext ui`'s Cron panel can show last status, success/failure counts and a recent-run feed.",
          "type": "boolean"
        },
        "historyMaxRuns": {
          "description": "Runs kept per task before the oldest are dropped (default 500).",
          "type": "number",
          "minimum": 1
        }
      }
    },
    "images": {
      "description": "Image-optimization config. Remote sources are refused by default (local-only, SSRF-safe); allowlist hosts here to enable optimizing remote images.",
      "type": "object",
      "properties": {
        "unoptimized": {
          "description": "Render every `<Image>` as a plain `<img>` with its raw `src` — no optimization, no generated `srcSet` (matches Next's `images.unoptimized`). `<Image>` optimizes by default; set this to opt the whole app out. Static export forces this on (there is no server to optimize against). Per-image, use the `unoptimized` prop.",
          "type": "boolean"
        },
        "domains": {
          "description": "Exact remote hosts allowed as sources (host only, e.g. `cdn.example.com`).",
          "deprecated": true,
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "remotePatterns": {
          "description": "Pattern-based remote allowlist (protocol/host-wildcard/pathname).",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "protocol": {
                "description": "Required protocol, e.g. `\"https\"`. Any protocol when omitted.",
                "type": "string"
              },
              "hostname": {
                "description": "Host to match: exact (`cdn.example.com`) or wildcard (`*.example.com`).",
                "type": "string"
              },
              "pathname": {
                "description": "Pathname the source must match: a Next.js glob (`/images/**`, `*` = one segment, `**` = any depth). A bare prefix (`/images/`) matches that path and anything below it. Any when omitted.",
                "type": "string"
              },
              "port": {
                "description": "Required port (e.g. `\"3000\"`); the protocol default when omitted.",
                "type": "string"
              },
              "search": {
                "description": "Required query string (e.g. `\"?v=1\"`); any when omitted.",
                "type": "string"
              }
            },
            "required": [
              "hostname"
            ]
          }
        },
        "localPatterns": {
          "description": "Allowed **local** source patterns (pathname glob + optional exact query). When set, a `public/` source matching none is refused (400); when omitted, all local sources are allowed (the default). Mirrors Next.js `images.localPatterns`.",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "pathname": {
                "description": "Pathname glob the local source must match (e.g. `/assets/**`). Any when omitted.",
                "type": "string"
              },
              "search": {
                "description": "Exact query string required (e.g. `\"v=1\"`), or `\"\"` for none. Any when omitted.",
                "type": "string"
              }
            }
          }
        },
        "deviceSizes": {
          "description": "Allowed responsive breakpoint widths for full-width images (matches Next's `images.deviceSizes`). The `/_denext/image` endpoint only honors `w=` values drawn from `deviceSizes ∪ imageSizes`; any other width is refused (400). This bounds the endpoint's distinct-work surface — without it, an attacker can request thousands of arbitrary widths, each forcing a fresh WASM decode/resize. Defaults to Next's standard set.",
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "imageSizes": {
          "description": "Allowed fixed widths for smaller images (icons, thumbnails) — matches Next's `images.imageSizes`. Unioned with ImagesConfig.deviceSizes to form the `/_denext/image` width allowlist. Defaults to Next's standard set.",
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "qualities": {
          "description": "Allowed `q=` quality values (matches Next.js 16 `images.qualities`). The endpoint refuses any other quality (400), bounding the distinct-encode surface the same way deviceSizes bounds widths. Defaults to `[75]`.",
          "type": "array",
          "items": {
            "type": "number"
          }
        },
        "minimumCacheTTL": {
          "description": "Minimum seconds to cache an optimized image (`Cache-Control: max-age`). Mirrors Next.js `images.minimumCacheTTL`. Defaults to `14400` (4 hours).",
          "type": "number",
          "minimum": 0
        },
        "formats": {
          "description": "Output formats the endpoint may negotiate from the request `Accept` header, in preference order (matches Next.js `images.formats`). Include `\"image/avif\"` to enable AVIF (falls back to WebP when the client doesn't accept AVIF). Defaults to `[\"image/webp\"]`.",
          "type": "array",
          "items": {
            "enum": [
              "image/webp",
              "image/avif"
            ]
          }
        },
        "maximumRedirects": {
          "description": "Max redirect hops to follow for a remote source, each re-validated (matches Next.js `images.maximumRedirects`). Defaults to `3`; `0` disables redirects.",
          "type": "number",
          "minimum": 0
        },
        "dangerouslyAllowLocalIP": {
          "description": "**Dangerous.** Allow remote sources that resolve to loopback/private/link-local addresses, disabling the SSRF address guard for the image optimizer (Next.js 16 `images.dangerouslyAllowLocalIP`). Only enable in a trusted, isolated network where the optimizer cannot reach internal services. Defaults to `false`.",
          "type": "boolean"
        }
      }
    },
    "tailwind": {
      "description": "Tailwind CSS integration. When set, denext manages the Tailwind v4 standalone binary and compiles `input` → `output` automatically on `dev`/`build`.",
      "type": "object",
      "properties": {
        "input": {
          "description": "Input stylesheet with the Tailwind directives, relative to the project root.",
          "type": "string"
        },
        "output": {
          "description": "Compiled stylesheet to emit (imported by your layout), relative to the root.",
          "type": "string"
        }
      },
      "required": [
        "input",
        "output"
      ]
    },
    "mdx": {
      "description": "MDX compilation options for `.mdx`/`.md` sources in a compat (npm-React) app. The baseline loader compiles plain MDX/CommonMark; set this to thread app-configured unified plugins (e.g. Codehike, GFM, syntax highlighting) into MDX's `compile`. Because `denext.config.ts` is a real module, the plugins are imported and passed as function references, not named as strings.",
      "type": "object",
      "properties": {
        "remarkPlugins": {
          "description": "remark (Markdown AST) plugins — e.g. `remark-gfm`, `remarkCodeHike`.",
          "type": "array"
        },
        "rehypePlugins": {
          "description": "rehype (HTML AST) plugins — e.g. `rehype-slug`, `rehype-pretty-code`.",
          "type": "array"
        },
        "recmaPlugins": {
          "description": "recma (JS AST) plugins — e.g. `recmaCodeHike`.",
          "type": "array"
        },
        "remarkRehypeOptions": {
          "description": "Options forwarded to MDX's `remark-rehype` bridge (`remarkRehypeOptions`).",
          "type": "object",
          "additionalProperties": {}
        },
        "providerImportSource": {
          "description": "MDX `providerImportSource` (module exporting `useMDXComponents`), if used.",
          "type": "string"
        }
      }
    },
    "cache": {
      "description": "Cache Components / ISR data + page cache store. Omit to let denext resolve the default at startup — the durable `node:sqlite` store when a writable filesystem is available, else the in-memory store. Set CacheConfig.store to force a choice, or pass your own CacheStore.",
      "type": "object",
      "properties": {
        "store": {
          "description": "Which store backs `use cache` / ISR. `\"sqlite\"` = the durable `node:sqlite` file store (real SQLite, built into Deno, zero-npm); `\"memory\"` = the in-process LRU store (ephemeral, per-process); or a custom CacheStore. Omit for the smart default: `node:sqlite` when a writable FS is available, otherwise in-memory.",
          "anyOf": [
            {
              "enum": [
                "sqlite"
              ]
            },
            {
              "enum": [
                "memory"
              ]
            },
            {
              "type": "object",
              "properties": {
                "getData": {
                  "description": "Return a fresh data entry for `key`, or undefined if missing/stale."
                },
                "setData": {
                  "description": "Store a data entry under `key`."
                },
                "getPage": {
                  "description": "Return a fresh cached page for `key`, or undefined if missing/stale."
                },
                "setPage": {
                  "description": "Store a cached page under `key`."
                },
                "deleteByTag": {
                  "description": "Purge every data entry and page carrying `tag`."
                },
                "deleteByPath": {
                  "description": "Purge every cached page rendered for `path` (an exact pathname)."
                },
                "expireByTag": {
                  "description": "Optional: **soft-expire** every entry carrying `tag` — rewrite its `staleAt`/`expiresAt` (stale-while-revalidate) instead of deleting it, so a `revalidateTag(tag, profile)` serves stale while refreshing. A store that omits this falls back to a hard deleteByTag (correct, just not SWR)."
                }
              },
              "required": [
                "getData",
                "setData",
                "getPage",
                "setPage",
                "deleteByTag",
                "deleteByPath"
              ]
            }
          ]
        },
        "path": {
          "description": "SQLite store file path (default `.denext/cache.db`).",
          "type": "string"
        },
        "maxDataEntries": {
          "description": "Max rows in the durable data cache before FIFO eviction (default 1000).",
          "type": "number",
          "minimum": 1
        },
        "maxPageEntries": {
          "description": "Max rows in the durable page (ISR) cache before FIFO eviction (default 1000).",
          "type": "number",
          "minimum": 1
        }
      }
    },
    "hsts": {
      "description": "`Strict-Transport-Security` (HSTS) header tuning, applied to responses served over HTTPS. Defaults to `max-age=31536000` (1 year, host-only — no `includeSubDomains`/`preload`, a safe default that can't brick sibling subdomains). Set fields to opt into a stronger policy, or `false` to omit the header entirely (e.g. when your edge sets it).",
      "anyOf": [
        {
          "type": "object",
          "properties": {
            "maxAge": {
              "description": "`max-age` in seconds (how long browsers pin HTTPS). Default `31536000` (1 year).",
              "type": "number",
              "minimum": 0
            },
            "includeSubDomains": {
              "description": "Add `includeSubDomains` (applies HSTS to every subdomain — enable only when all are HTTPS).",
              "type": "boolean"
            },
            "preload": {
              "description": "Add `preload` (eligibility for browser HSTS preload lists; requires `includeSubDomains`).",
              "type": "boolean"
            }
          }
        },
        {
          "enum": [
            false
          ]
        }
      ]
    },
    "csp": {
      "description": "App-wide Content-Security-Policy default (three-state), overridable per file: - `\"strict\"` (default) — denext's hash-based strict policy on buffered pages. - `\"off\"` — emit no CSP header at all (set your policy at the edge, or for Next.js-style \"CSP is the app's job\" behavior). A route can still opt back in with its own `csp` export. - a CspSetting object — the strict policy plus these global opt-ins.",
      "anyOf": [
        {
          "enum": [
            "strict"
          ]
        },
        {
          "enum": [
            "off"
          ]
        },
        {
          "type": "object",
          "properties": {
            "scriptSrc": {
              "description": "Extra `script-src` sources (external script hosts).",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "styleSrc": {
              "description": "Extra `style-src` sources (external stylesheet hosts).",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "imgSrc": {
              "description": "Extra `img-src` sources (`<Image>` is already same-origin via the optimizer).",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "connectSrc": {
              "description": "Extra `connect-src` sources (fetch/XHR/EventSource/WebSocket targets).",
              "type": "array",
              "items": {
                "type": "string"
              }
            }
          }
        }
      ]
    },
    "publicEnv": {
      "description": "Public-env vars to always embed in the page island, in addition to the ones the build detects the client references. Use this to force-include a key the client reads via a computed expression (`publicEnv()[\"NEXT_PUBLIC_\" + x]`), which the build can't see. Referenced keys are shipped automatically; this only adds to that set.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "streaming": {
      "description": "Incremental (Suspense) streaming, **on by default**; set `false` to opt out. A page with a pending Suspense boundary flushes its shell first and streams each boundary's content as it resolves; a fully synchronous page (no holes) is still delivered buffered, so it stays shared-cacheable. Streamed responses carry the same strict hash-based CSP as buffered ones (the swap runtime is a hashed constant), survive a failing boundary (its fallback stays), and cover Flight routes. ISR/PPR-cacheable routes (revalidate/force-static) and soft navigations take their own path first, so streaming never bypasses the page cache. A shipped, default-on capability — not an experiment.",
      "type": "boolean",
      "default": true
    },
    "live": {
      "description": "Live Server Components security policy: authorization hooks and resource caps for the `<Live>` / `useLive` / `usePresence` WebSocket hub. See LiveConfig. Presence/data are default-deny in production without a policy here — so this is a **security-policy** field, not an on/off experiment.",
      "type": "object",
      "properties": {
        "allowAnonymous": {
          "description": "Permit presence-room joins with no policy hook — opens rooms to any same-origin client. It does **not** open arbitrary data: `useLive` data subscriptions still require the per-action `liveReadable(...)` opt-in (or a `canSubscribe` hook), so enabling anonymous presence never exposes unmarked/mutating actions on the socket. Only set it for genuinely public collaboration. Defaults to `false` (deny in dev and production alike).",
          "type": "boolean"
        },
        "authorize": {
          "description": "Gate the WebSocket connection itself (after the same-origin handshake check)."
        },
        "canJoinRoom": {
          "description": "Gate a presence-room join/update. Return `false` to refuse the room."
        },
        "canSubscribe": {
          "description": "Gate a `useLive` data subscription (which action + args it may run)."
        },
        "canWatchTags": {
          "description": "Gate a `useApi({ tags })` tag watch: may this viewer be TOLD that `tags` were invalidated? The socket carries only tag names (the client then refetches over HTTP with its own cookies, so the route handler still authorizes the data). Under `allowAnonymous` every same-origin client may watch; without either it is a `no-policy` refusal."
        },
        "limits": {
          "description": "Resource caps for the hub.",
          "type": "object",
          "properties": {
            "maxConnections": {
              "description": "Max simultaneous connections (default 10000).",
              "type": "number"
            },
            "maxSubscriptionsPerConnection": {
              "description": "Max `useLive` subscriptions per connection (default 64).",
              "type": "number"
            },
            "maxRoomsPerConnection": {
              "description": "Max presence rooms per connection (default 32).",
              "type": "number"
            },
            "maxPeersPerRoom": {
              "description": "Max peers in one presence room (default 1000). A room's membership is otherwise bounded only by `maxConnections`, and `broadcastRoom` re-encodes O(N) bytes to N peers on every join/update/leave, so an unbounded room is O(N²) fan-out fleet-wide — a join past the cap is refused with `limit`.",
              "type": "number"
            },
            "maxBoundaries": {
              "description": "Max `<Live>` boundaries watched per connection (default 256).",
              "type": "number"
            },
            "maxMessageBytes": {
              "description": "Max inbound message size in bytes (default 65536).",
              "type": "number"
            },
            "maxSubscriptionInputBytes": {
              "description": "Max size in bytes of one subscription's input / args (default 16384): the input is stored for the connection's lifetime and re-used on every recompute, so it is capped tighter than a frame.",
              "type": "number"
            },
            "maxChannelsPerConnection": {
              "description": "Max `useChannel` subscriptions per connection (default 32).",
              "type": "number"
            },
            "maxChannelPayloadBytes": {
              "description": "Max bytes of one channel payload (default 16384); `publish` throws past it.",
              "type": "number"
            },
            "channelAuthTtlSeconds": {
              "description": "Seconds after which a channel subscriber is lazily re-authorized on the next push (default 300). Per-push re-authorization would cost subscribers × `authorize` per emit; `channel.revoke(key)` ends access immediately when that matters.",
              "type": "number"
            },
            "idleTimeoutSeconds": {
              "description": "Socket idle timeout in seconds passed to `Deno.upgradeWebSocket` (default 120).",
              "type": "number"
            },
            "maxConcurrentRenders": {
              "description": "Fleet-wide cap on concurrent live re-renders/recomputes (default 40). A single `revalidateTag` can match every connected socket; this bounds how many full-route re-renders (`<Live>`) and `useLive` fetcher runs execute at once so one invalidation can't spawn one render per connection simultaneously (a self-inflicted DoS amplification). Excess work queues and drains as slots free.",
              "type": "number"
            },
            "renderTimeoutSeconds": {
              "description": "Per-render deadline in seconds (default 30). A `<Live>` re-render or `useLive` fetcher run holds one of the `maxConcurrentRenders` slots for its whole duration; without a deadline a hung user fetcher pins its slot forever, and enough hung fetchers peg the concurrency gate and stall the whole live fleet. On timeout the run is aborted (a cooperative `AbortSignal` reaches the fetcher's `fetch`/cache reads) and its slot released; the client gets an error/refresh frame. On by default — there is no \"disable\" value (an invalid one falls back to 30s).",
              "type": "number"
            }
          }
        }
      }
    },
    "apiBatch": {
      "description": "The typed-API batch endpoint (`POST /_denext/api-batch`: N GET/HEAD calls from `createApiClient` in one round trip). Same-origin only; caps on items, body, fan-out concurrency, and per-item response size. See ApiBatchConfig.",
      "type": "object",
      "properties": {
        "enabled": {
          "description": "Serve the endpoint at all (default true; `false` → 404).",
          "type": "boolean",
          "default": true
        },
        "maxItems": {
          "description": "Max items per batch (default 20, at most 100).",
          "type": "number",
          "minimum": 1,
          "maximum": 100
        },
        "maxBodyBytes": {
          "description": "Max batch request body in bytes (default 1 MiB).",
          "type": "number",
          "minimum": 1
        },
        "concurrency": {
          "description": "Items run concurrently per batch (default 4).",
          "type": "number",
          "minimum": 1,
          "maximum": 64
        },
        "maxItemResponseBytes": {
          "description": "Max bytes of one item's response body carried back (default 4 MiB; over → a 500 item).",
          "type": "number",
          "minimum": 1
        },
        "maxTotalResponseBytes": {
          "description": "Max bytes of ALL items' response bodies together (default 16 MiB; over → the rest are 500 items).",
          "type": "number",
          "minimum": 1
        }
      }
    },
    "apiMaxBodyBytes": {
      "description": "The request-body cap for route handlers (`route.ts`), in bytes — default 1 MiB. A route raises or lifts its own with `export const maxBodyBytes = N | false`; a `defineApi` endpoint with `maxBodyBytes` in its definition overrides both. Over the cap → 413.",
      "type": "number",
      "minimum": 1
    },
    "actionMaxBodyBytes": {
      "description": "The request-body cap for Server Actions, in bytes — default 1 MiB (Next's default). Raise it only for actions that accept large payloads (multipart uploads); over the cap → 413 before the action runs. `denext start`/`dev` forward it to the server; a custom server passes `actionMaxBodyBytes` to `createApp()` itself.",
      "type": "number",
      "minimum": 1
    },
    "canonicalOrigin": {
      "description": "The app's public origin (e.g. `\"https://example.com\"`), pinned outright: absolute URLs (canonical, `og:image`), the Server Action origin check and HSTS all use it instead of the `Host` / forwarded headers — the robust choice behind a proxy that rewrites `Host`. A full origin (scheme + host, no path). Unset, the `DENEXT_CANONICAL_ORIGIN` env var is read at boot (config > env > derived from the request).",
      "type": "string"
    },
    "trustForwardedHeaders": {
      "description": "Trust `X-Forwarded-Proto` / `X-Forwarded-Host` from a reverse proxy when deriving the request origin (absolute URLs, the Server Action origin check, HSTS), and `X-Forwarded-For` / an inbound `X-Request-Id` for `clientIp()`, `requestId()` and the rate limiters (`denextAuth` inherits it unless it sets its own). Enable ONLY when clients cannot reach denext directly — a client that can spoofs them. The origin part is ignored when DenextConfig.canonicalOrigin is set. Unset, `DENEXT_TRUST_PROXY=1` turns it on (config > env > `false`).",
      "type": "boolean"
    },
    "allowedDevOrigins": {
      "description": "`denext dev` only: extra hosts allowed to reach the dev server's `/_denext/*` assets (the bundles, the module graph, the reload stream, the Live hub), beyond loopback. Each entry is an origin (`\"http://192.168.1.5:3000\"`) or a bare host (`\"192.168.1.5\"`, `\"mac.local\"`, `\"mac.local:3000\"`); matching is on the hostname. Wildcards are not supported: list each host. Mirrors Next.js's `allowedDevOrigins`.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "requestTimeout": {
      "description": "Per-request deadline in milliseconds — default 30 000. A request still running past it is aborted and answered `503`; the per-request `AbortSignal` fires so cooperative work (`fetch(url, { signal })`) cancels. `0` disables the deadline. Unset, the `DENEXT_REQUEST_TIMEOUT_MS` env var is read at boot (config > env > default).",
      "type": "number",
      "minimum": 0
    },
    "maxConcurrency": {
      "description": "In-process concurrency ceiling: the max number of client requests one instance handles at once. A request arriving at capacity is shed immediately with `503` + `Retry-After` (never queued). Bounds work up to the point the `Response` is produced (a streaming body's client-read time is not counted); background ISR regeneration is exempt. A complement to — not a replacement for — the edge/load-balancer ceiling. Default: no limit. Unset, the `DENEXT_MAX_CONCURRENCY` env var is read at boot (config > env > unlimited).",
      "type": "number",
      "minimum": 1
    },
    "slotBackstop": {
      "description": "With DenextConfig.maxConcurrency set and DenextConfig.requestTimeout `0`, the milliseconds after which a never-settling request's concurrency slot is force-freed (default 120 000). It frees only the slot — the render is not aborted, since the request timeout was opted out of. Inert while a request timeout is in place.",
      "type": "number",
      "minimum": 1
    },
    "cacheKeyParams": {
      "description": "Allowlist of query-parameter names that fork the ISR page-cache key. When set, only these params make a distinct cached entry; every other param (`?utm_*`, `?fbclid`, a random cache-buster) is ignored for keying — but still reaches the render via `searchParams`, so list every param whose value changes cacheable output. Unset, every param participates.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "nodeResolve": {
      "description": "denext's tolerant node_modules resolver for the compat (npm-React) build — default ON.",
      "type": "boolean",
      "default": true
    },
    "cacheComponents": {
      "description": "Cache Components (Next.js 16): the `\"use cache\"` directive is compiled into cross-request caching on the server (`src/build/use-cache-transform.ts`), plus the PPR render path — dynamic-by-default rendering with cacheable `use cache` islands (a cached shell with per-request dynamic holes spliced in). A stable, **opt-in** feature, deliberately **off by default**: caching stays a choice, because a cache bug's failure class (a poisoned or cross-user shell) is severe and must not be imposed on apps that never asked. When off, every `\"use cache\"` directive is inert (a plain no-op string statement) and rendering is unchanged.",
      "type": "boolean"
    },
    "reactCompiler": {
      "description": "The build-time auto-memoization compiler (a React-Compiler-style pass), an opt-in optimization that is off by default. Conservative by construction: it bails to identity whenever a transform isn't provably safe, so it only ever adds memoization, never changes behavior.",
      "type": "boolean"
    },
    "asyncContext": {
      "description": "Scope async `startTransition` by transition IDENTITY instead of a time window, so a post-`await` update is attributed to its transition while an unrelated urgent update in the pending window keeps its priority. Off by default.",
      "type": "boolean"
    },
    "features": {
      "description": "Compile-time feature flags: `feature(\"KEY\")` (from `denext/feature`) always returns the configured value, and a call with a string-literal KEY is folded to a literal where the build can, so the bundler dead-code-eliminates the untaken branch. A key not listed reads `false`.",
      "type": "object",
      "additionalProperties": {
        "type": "boolean"
      }
    },
    "optimizePackageImports": {
      "description": "npm packages whose barrel (`index`) imports are rewritten to the files that define each name, as Next.js's `optimizePackageImports` does: `import { Check } from \"lucide-react\"` becomes an import of `lucide-react`'s `icons/check.js`, so the bundler never loads the barrel. Your list is ADDED to a built-in default (lucide-react, date-fns, lodash-es, …); a `\"!pkg\"` entry removes a package from it, and `false` turns the optimization off.",
      "anyOf": [
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        {
          "enum": [
            false
          ]
        }
      ]
    },
    "momentumSafeScroll": {
      "description": "Keep iOS momentum scrolling alive while scroll-anchoring code corrects the scroll offset — default ON. In iOS WebKit (Safari, WKWebView, Capacitor) any programmatic scroll write during a touch fling (`scrollBy`, `scrollTo`, assigning `scrollTop`) stops the fling dead, and virtualized lists (LegendList, react-virtuoso, TanStack Virtual) issue exactly such writes to compensate for rows measured taller or shorter than estimated. The client runtime therefore defers those writes during a gesture (see `installMomentumSafeScroll` in `denext/mobile`) and applies them in one step once the scroller comes to rest.",
      "type": "boolean",
      "default": true
    },
    "reactNative": {
      "description": "Build a React Native / Expo app's source for the web through `react-native-web` — SPA mode only (`mode: \"spa\"`). `true` turns on the defaults; an object sets options.",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "object",
          "properties": {
            "rootStyle": {
              "description": "Inject Expo web's root style into the SPA shell's `<head>`: `html,body,#root{height:100%;margin:0}` and `#root{display:flex}` (`#root` follows `spa.rootId`). React Native's root view is `flex: 1` and needs a sized flex parent; without it the app collapses to its content height and absolutely positioned overlays cover the page. It comes before `spa.head`, so a rule there overrides it.",
              "type": "boolean",
              "default": true
            },
            "expoShims": {
              "description": "Alias each `expo-*` package that has a `denext/expo/*` shim (`expo-haptics` → `denext/expo/haptics`, …: the list is `denext/expo/manifest`'s `EXPO_SHIMS`) to that shim, which implements the package's API over `denext/mobile` and web APIs. A package without a shim resolves normally. `false` resolves every `expo-*` package normally.",
              "type": "boolean",
              "default": true
            }
          }
        }
      ]
    },
    "experimental": {
      "description": "Deprecated. Every `experimental.*` key graduated to a top-level field by 2.5 (`reactCompiler`, `asyncContext`, `features`, `nodeResolve`, `cacheComponents`). The old spellings are still honored, with a dev warning, when the top-level field is absent; the top-level field wins when both are set. Removed in 3.0.",
      "deprecated": true,
      "type": "object",
      "properties": {
        "reactCompiler": {
          "description": "Deprecated. Graduated to the top-level `reactCompiler` in 2.5. Honored as an alias through 2.x; removed in 3.0.",
          "deprecated": true,
          "type": "boolean"
        },
        "compiler": {
          "description": "Deprecated. Renamed `reactCompiler` (Next.js's key) in 2.0, now the top-level `reactCompiler`. Honored as an alias through 2.x; removed in 3.0.",
          "deprecated": true,
          "type": "boolean"
        },
        "asyncContext": {
          "description": "Deprecated. Graduated to the top-level `asyncContext` in 2.5. Honored as an alias through 2.x; removed in 3.0.",
          "deprecated": true,
          "type": "boolean"
        },
        "features": {
          "description": "Deprecated. Graduated to the top-level `features` in 2.5. Honored as an alias through 2.x; removed in 3.0.",
          "deprecated": true,
          "type": "object",
          "additionalProperties": {
            "type": "boolean"
          }
        },
        "nodeResolve": {
          "description": "Deprecated. Moved to the top-level `nodeResolve` in 2.0 — it is load-bearing for every compat migration, not an incomplete feature. Honored as an alias through 2.x.",
          "deprecated": true,
          "type": "boolean"
        },
        "optimizePackageImports": {
          "description": "Deprecated. Next.js's spelling of the top-level `optimizePackageImports`. Honored as an alias (with a dev warning) when the top-level field is absent.",
          "deprecated": true,
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "classComponents": {
      "description": "How the client bundle gets the React class-component runtime (`class X extends React.Component`: lifecycle, setState batching, class error boundaries). It is a code-split chunk (`denext/class-runtime`), so the default needs no configuration. Unset, `denext build` scans your sources and installs it eagerly when a class is found, otherwise a page loads it on demand; `true` always installs it eagerly; `false` never ships it — the runtime is dead-code-eliminated and a class used anyway throws a guided error.",
      "type": "boolean"
    },
    "compatibilityMode": {
      "description": "Run the app through the **next-compat** SSR/client pipeline, which rewrites every `react`/`react-dom`/`next/*` import (including those inside npm React libraries) to denext at bundle time so the whole app runs on one denext React — the drop-in path for real Next.js App Router projects. `true`/`false` force it; the default `\"auto\"` enables it when `node_modules/react` exists or `package.json` lists `react`/`next`. A pure denext-native app keeps the zero-overhead source-load path.",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "enum": [
            "auto"
          ]
        }
      ]
    },
    "plugins": {
      "description": "denext plugins (e.g. a Pages Router). Each is set up once before routes are scanned and may contribute routes, claim requests, and emit build assets — see DenextPlugin. Apps with no plugins pay nothing.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "description": "Unique plugin name (used to de-duplicate registration across a process).",
            "type": "string"
          },
          "setup": {
            "description": "Wire the plugin into denext's seams. Runs once, before routes are scanned."
          }
        },
        "required": [
          "name",
          "setup"
        ]
      }
    },
    "commands": {
      "description": "Project-local CLI verbs: `denext <name>` runs the entry's `run`, with the same flag parsing, `--help` rendering, and \"did you mean\" suggestions a built-in verb gets. The shorthand for a one-off project script — a plugin (`addCommand`) is only needed when the verb ships as a reusable package.",
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "description": "The verb, e.g. `\"seed\"` for `denext seed`. Lowercase; `[a-z][a-z0-9-]*`.",
            "type": "string"
          },
          "summary": {
            "description": "One-line summary shown by `denext commands` under \"Project commands\".",
            "type": "string"
          },
          "usage": {
            "description": "Optional multi-line detail shown by `denext <name> --help`.",
            "type": "string"
          },
          "flags": {
            "description": "Declarative flags (parsed, defaulted, and documented like a built-in verb's).",
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "name": {
                  "description": "Long name without the leading `--` (e.g. `\"port\"` for `--port`).",
                  "type": "string"
                },
                "alias": {
                  "description": "Optional single-character alias without the leading `-` (e.g. `\"p\"`).",
                  "type": "string"
                },
                "altNames": {
                  "description": "Additional long names that resolve to this flag (e.g. `[\"hostname\"]`).",
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                },
                "type": {
                  "description": "Value kind. `boolean` flags take no value (presence = `true`).",
                  "enum": [
                    "boolean",
                    "string",
                    "number"
                  ]
                },
                "default": {
                  "description": "Default applied when the flag is absent.",
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    },
                    {
                      "type": "boolean"
                    }
                  ]
                },
                "help": {
                  "description": "One-line help shown under `--help`.",
                  "type": "string"
                },
                "valueName": {
                  "description": "Value placeholder for help/usage of a valued flag (e.g. `\"<port>\"`).",
                  "type": "string"
                },
                "repeatable": {
                  "description": "A `string` flag that may be given more than once: every value is kept, joined with `,` (`--x a --x b` reads as `\"a,b\"`), so a repeated flag and a comma-separated one read the same. Without it the last value wins.",
                  "type": "boolean"
                }
              },
              "required": [
                "name",
                "type",
                "help"
              ]
            }
          },
          "positionals": {
            "description": "Declarative positionals (for help/usage; parsing collects all positionals).",
            "type": "array",
            "items": {
              "type": "object",
              "properties": {
                "name": {
                  "description": "Display name (e.g. `\"dir\"`).",
                  "type": "string"
                },
                "help": {
                  "description": "One-line help.",
                  "type": "string"
                },
                "required": {
                  "description": "Whether omitting it is an error.",
                  "type": "boolean"
                },
                "variadic": {
                  "description": "Whether it soaks up all remaining positionals.",
                  "type": "boolean"
                }
              },
              "required": [
                "name",
                "help"
              ]
            }
          },
          "run": {
            "description": "The implementation, handed the parsed invocation."
          }
        },
        "required": [
          "name",
          "summary",
          "run"
        ]
      }
    }
  },
  "additionalProperties": false
}
