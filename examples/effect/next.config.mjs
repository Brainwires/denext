/** @type {import("next").NextConfig} */
const nextConfig = {
  // A honored key so the migration has something real to translate into denext.config.ts.
  trailingSlash: true,
};

export default nextConfig;
